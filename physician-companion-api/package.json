{
  "name": "physician-companion",
  "private": true,
  "version": "0.1.0",
  "description": "Physician Mental Health Companion — full stack (API + Peer Beacon UI)",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "dev:api": "npm run dev -w backend",
    "dev:web": "npm run dev -w frontend",
    "build": "npm run build -w backend && npm run build -w frontend",
    "lint": "npm run lint -w backend && npm run lint -w frontend",
    "db:generate": "npm run db:generate -w backend",
    "db:migrate": "npm run db:migrate -w backend",
    "db:seed": "npm run db:seed -w backend",
    "docker:up": "docker compose -f backend/docker-compose.yml up -d",
    "worker": "npm run dev:worker -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
