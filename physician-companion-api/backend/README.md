# Physician Companion API

See the [monorepo README](../README.md) for full-stack setup.

## Backend-only commands

```bash
npm run dev
npm run dev:worker
npm run db:migrate
npm run db:seed
```

API listens on port **3000** (`API_PREFIX=/api/v1`).
